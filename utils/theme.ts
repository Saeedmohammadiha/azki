import { createTheme } from "@mui/material";

export const theme = createTheme({
    typography: {
      fontFamily: "inherit",
    },
    palette: {
      success: {
        main: "#25b79b",
        light: "#25b79b",
        dark: "#25b79b",
      },
    },
  });